import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Chip = () => {
  return (
    <View>
      <Text>Chip</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Chip;
